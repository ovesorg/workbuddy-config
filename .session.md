# Session Log

## 2026-05-20

- Created `journal`, `repo-wrap`, and `workbuddy-wrap` skills in `~/.workbuddy/skills/` to align with Codex workspace conventions
- Verified authoritative source from `C:\Users\Huashan_Wang\.codex\skills/repo-session-wrap/` and `.codex/skills/journal/`
- Created `workbuddy-wrap/references/memory-locations.md` decision guide
- Created `~/.workbuddy/skills/workbuddy-wrap/` — WorkBuddy's own session wrap skill
- Updated MEMORY.md with full skill architecture

## 2026-05-19

- Fixed nav style: removed `navigation.tabs` and `navigation.sections` from `mkdocs.yml` to match consolidated-docs flat nav
- Created `doc-site-template` skill (`~/.workbuddy/skills/doc-site-template/`) —固化三步流程（migrate → normalize → krr）
- Created `connectors/workspace-mcp/` connector with `mcp.json` + `SKILL.md`
- Created `docs/agent/workbuddy-setup.md` — AI-agent-readable setup doc (MCP configs, env vars, bootstrap workflow, .workbuddy structure)
- Updated `mkdocs.yml` nav to include WorkBuddy Setup (Agent) entry
- Commits: `2a67f27` (workspace-mcp + agent docs), `1117955` (mkdocs-template normalization), `2eb6b9b` (6 categories)
